---
"@app/builder": minor
---

Phase X7: the field property panel can author the new cross-cutting decorator extras —
an "Extra hint" input (persistent `Form.Item` hint) and a "Show feedback" checkbox
(`hasFeedback`). Both are offered on leaf fields only (arrays render their own shell).
