---
"@org/form-core": patch
---

Add unit tests covering `isVisible` (JSONLogic true/false branches) and `canView`/`canEdit`
(RBAC view/edit role matching), plus the vitest test tooling for the package.
