---
"@org/form-schema": minor
---

Add a `textarea` leaf field type (with optional `placeholder`/`maxLength`/`rows`). Purely
additive — existing schemas and migrations are unaffected, no `CURRENT_FORM_VERSION` bump.
