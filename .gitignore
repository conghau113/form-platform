# dependencies
node_modules/

# build output
dist/
*.tsbuildinfo

# turborepo
.turbo/

# playwright e2e (E7) — generated artifacts (all created at the repo root)
/playwright-report/
/test-results/
/blob-report/
/.playwright/

# vite
**/.vite/

# runtime data (api file store)
.data/

# secrets / env (NEVER commit; .env.example IS committed)
.env
.env.*
!.env.example

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# editor / OS
.DS_Store
Thumbs.db
.idea/
*.local

# windows / git-bash crash dumps
*.stackdump

# local scratch / AI helper output (not part of the project)
project-tree-for-ai.txt
structure.txt
