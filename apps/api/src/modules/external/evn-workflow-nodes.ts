// GENERATED by src/scripts/measure-evn-workflow.ts — do not edit by hand.
// Measured on 2026-08-14 from EVN core-service @ 9ff6385f:
//   public/files/templateJSON/WORKFLOW_PCT.json                    (the 15 PCT workflow nodes)
//   src/modules/ticket/service/workflow.service.ts (how EVN evaluates and stores them)
//   src/modules/ticket/workflow.enum.ts            (EWorkflowNodeEdgeStatus, EWorkflowConditionCode)
// 15 nodes; `complete` forms: ACTION_CODE×12, ANY_ACTION_CODES×2, NODE_COMPLETED×1.

/** One node of EVN's PCT workflow, as their template defines it. */
export interface EvnWorkflowNodeDef {
  /** `formItems[].code`, and also the key EVN stores this node's state under. */
  readonly nodeCode: string;
  /** `priority` — a permutation of 1..15, and the order the flow reads in. */
  readonly order: number;
  /**
   * The action code(s) whose arrival completes this node, read from `condition.complete`.
   *
   * `[]` ONLY when {@link completedByNode} is set. An empty list with no `completedByNode` would
   * be unreadable: "no action closes this" and "we failed to extract it" look identical.
   */
  readonly completedBy: readonly string[];
  /**
   * Set INSTEAD of {@link completedBy} when the node closes on another NODE rather than an action
   * (`NODE_COMPLETED`). One node does this today: `FINISHED`.
   */
  readonly completedByNode?: string;
}

/**
 * EVN's PCT workflow nodes, in flow order.
 *
 * ⚠️ This table is READ to build the `progress` projection, so it is part of
 * `PCT_DEFINITION` and moves `EVN_PCT_DEFINITION_VERSION` when it changes. It takes no part in
 * `allowed` or `nextStatus` — those come from the transition table alone.
 *
 * ⚠️ Completion is measured from `condition.complete`, NOT from the node's name. Stripping the
 * `PCT_WORKFLOW_NODE__` prefix reproduces 12 of these 15 and is wrong for three — `STARTED`,
 * `PCT_A_CREATED` and `FINISHED` — which is exactly the kind of near-miss that ships.
 */
export const EVN_PCT_WORKFLOW_NODES: readonly EvnWorkflowNodeDef[] = [
  {
    nodeCode: "PCT_WORKFLOW_NODE__STARTED",
    order: 1,
    completedBy: ["PCT_A_CREATED", "PCT_A_UPDATE", "PCT_A_EDIT"],
  },
  {
    nodeCode: "PCT_WORKFLOW_NODE__PCT_A_CREATED",
    order: 2,
    completedBy: ["PCT_A_CREATED", "PCT_A_UPDATE", "PCT_A_EDIT"],
  },
  {
    nodeCode: "PCT_WORKFLOW_NODE__PCT_A_MODERATE_ALLOWER",
    order: 3,
    completedBy: ["PCT_A_MODERATE_ALLOWER"],
  },
  {
    nodeCode: "PCT_WORKFLOW_NODE__PCT_A_MODERATE_GSATD",
    order: 4,
    completedBy: ["PCT_A_MODERATE_GSATD"],
  },
  { nodeCode: "PCT_WORKFLOW_NODE__PCT_A_WORKING", order: 5, completedBy: ["PCT_A_WORKING"] },
  { nodeCode: "PCT_WORKFLOW_NODE__PCT_A_ALLOW", order: 6, completedBy: ["PCT_A_ALLOW"] },
  {
    nodeCode: "PCT_WORKFLOW_NODE__PCT_A_ALLOW_HANDOVER",
    order: 7,
    completedBy: ["PCT_A_ALLOW_HANDOVER"],
  },
  { nodeCode: "PCT_WORKFLOW_NODE__PCT_A_HANDOVER", order: 8, completedBy: ["PCT_A_HANDOVER"] },
  {
    nodeCode: "PCT_WORKFLOW_NODE__PCT_A_HANDOVER_APPROVED",
    order: 9,
    completedBy: ["PCT_A_HANDOVER_APPROVED"],
  },
  { nodeCode: "PCT_WORKFLOW_NODE__PCT_A_END", order: 10, completedBy: ["PCT_A_END"] },
  {
    nodeCode: "PCT_WORKFLOW_NODE__PCT_A_END_APPROVED",
    order: 11,
    completedBy: ["PCT_A_END_APPROVED"],
  },
  { nodeCode: "PCT_WORKFLOW_NODE__PCT_A_LOCK", order: 12, completedBy: ["PCT_A_LOCK"] },
  {
    nodeCode: "PCT_WORKFLOW_NODE__PCT_A_CONFIRM_LOCK",
    order: 13,
    completedBy: ["PCT_A_CONFIRM_LOCK"],
  },
  { nodeCode: "PCT_WORKFLOW_NODE__PCT_A_FINISHED", order: 14, completedBy: ["PCT_A_FINISHED"] },
  {
    nodeCode: "PCT_WORKFLOW_NODE__FINISHED",
    order: 15,
    completedBy: [],
    completedByNode: "PCT_WORKFLOW_NODE__PCT_A_FINISHED",
  },
];
