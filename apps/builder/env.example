# Copy to `.env` (gitignored) and fill in. Vite reads VITE_* at build time.

# Base URL of the @app/api server the builder talks to.
# Default when unset: http://localhost:3001 (see src/presets/config.ts)
VITE_API_BASE=http://localhost:3001
