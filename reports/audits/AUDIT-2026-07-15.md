# Framework audit — 2026-07-15

> **Nature:** Evidence (append-only, §5) · **Date:** 2026-07-15 · **Auditor:** agent (Opus 4.8)
> **Method:** the framework checks itself against the §13 health signals, each computed from existing artifacts (no new bookkeeping) · **Result:** healthy — enforcement posture strong, 0 undischarged manifest, 0 past-due freshness contract, 100% L3 traceability, 0 audit-escaped defect
> **Evidence class:** E1 (signals computed from existing artifacts / gate outputs — constitution §13)

The **first periodic self-audit** (constitution §13 Audit loop; roadmap E8 T8.1.2). It establishes the
**metrics baseline** (blueprint §9) that every future audit trends against. Inputs are the two clean E8
sweeps ([CALIBRATION-2026-07-15](../calibration/CALIBRATION-2026-07-15.md) +
[CONFORMANCE-2026-07-15](../conformance/CONFORMANCE-2026-07-15.md)), the two framework gates
(`check:index`, `check:traceability`), and `gate-inventory.yaml`. Only signals **computable from
existing artifacts** are measured (§13 — no new bookkeeping).

| Health signal (§13) | Value | Baseline / source |
|---|---|---|
| Share of constraints on ladder rung ≥2 | **19 gates, all rung ≥2** (18×r3 + 1×r2); ~76% incl. rung-1 golden rules | gate-inventory (12 CI + 6 local + 1 structural) |
| Oldest undischarged impact manifest (§9) | **none (0 open)** | no manifest filed; all E0–E7 normative changes discharged in-commit |
| Oldest past-due freshness contract (§10) | **0 past-due**; oldest contract = `system-overview` @ 2026-07-03 (12 days) | `check:index` health signal |
| Traceability coverage of L3 bindings | **12/12 = 100%** (72/72 relative links resolve) | `check:traceability` health signal |
| Escaped defects: found by audit vs by accident | **0 by audit / 3 by accident** (lifetime) | tracked findings (see below) |

## Signal detail

**1 — Ladder-rung share.** `gate-inventory.yaml` enumerates **19 enforcement gates, every one at
rung ≥2**: 12 CI machine-gates (rung 3), 6 local harness guardrails (rung 3 hook + denies), 1 structural
gate (`structure-feature-folders`, rung 2 enforced at 3 by `structure.test.ts`). The repo's *core*
checks are fully machine-enforced. The remaining constraints are the **rung-1 prose golden rules** (the
~6 product golden rules T6.1.1 reviewed — contract-first, formVersion/ADR-0014, one-schema-many-renderers,
no-eval/ADR-0015, renderer-peerDeps/ADR-0016, NestJS-runtime-import). Those are held at prose **by
design, not by neglect**: minimum-viable-rung (§14) climbs a constraint only on violation evidence, and
T6.1.1 found **0 lifetime violations** across them → gating would be speculative ceremony. Blended share
= 19 / (19+6) ≈ **76%**. Baseline reading: *enforcement is machine-complete where evidence warrants;
rule-climbing is correctly evidence-gated and paused.*

**2 — Undischarged impact manifests (§9).** No impact manifest is currently open. No standalone manifest
artifact has been filed (no `reports/manifests/`); across E0–E7 every normative change updated its
affected artifacts **within the same commit** (the manifest discharged inline), and the E8 sweeps
confirm no residue (0 drift, 0 violation). Baseline = **0 undischarged**.

**3 — Past-due freshness contracts (§10).** `check:index` reports the oldest `verified_on` as
`system-overview @ 2026-07-03` (12 days). All 13 cadences are **event-driven** ("per-change … else each
release"), not fixed intervals, and no release has been cut → **nothing is strictly past-due**. The E8
drift-sweep independently re-ran all 13 methods green today, so the whole set is freshly method-validated.
Baseline = **0 past-due**; oldest contract age **12 days**.

**4 — L3 traceability coverage.** `check:traceability`: **12/12 L3 bindings** (2 agents + 7 skills + 3
cursor rules) name an L1/L2 source; **72/72** relative markdown links across the governed surface (L3 +
governance/ + knowledge/) resolve. Baseline = **100%**.

**5 — Escaped defects (audit vs accident).** An escaped defect = one that got past the per-diff gates and
surfaced later. **This first audit found 0** (both sweeps clean — nothing escaped into the audit net).
Lifetime defects found **by accident / during work** (not by a periodic audit), from the tracked
findings: (a) the **FS3a NestJS DI bug** (`import type` erasing the injected `ConfigService` at runtime)
— caught by live-smoke in E5-CLOSE, not units; (b) **F1** — the working-copy `settings.json` dropping
the 4 `.env` read-denies — caught during the T6.2.3 review; (c) **stale doc figures** (production-hardening
"11 packages"→10; dotted `.env.example` SQLite-era duplicate) — caught during E2 build. Baseline ratio =
**0 : 3** (audit : accident). Detector false-positives (builder-fetch, eval-comment) are excluded — they
are calibration, not escaped defects. *Goal to trend: raise the audit share as the loop matures.*

## Findings

- No **new** defects surfaced by this audit — the framework's descriptive surface is true (drift-sweep
  13/13) and its load-bearing rules are obeyed (conformance-sweep 6/6). The health posture is strong.
- **Re-surfaced (already filed, not new):** F1 (`.env` denies dropped in working-copy `settings.json`,
  security — owner action) · F2 (gate-inventory `deny-secret-read` under-lists the committed `.env`
  denies) · production-hardening "11 packages"→10 · dotted `.env.example` stale.

## Escaped defects

None escaped into this audit. All 3 lifetime accident-found defects (Signal 5) were caught by the
per-change floors (live-smoke / review / build-time reads) *before* a periodic audit — evidence the
per-diff gates are doing their job, and the audit is confirming, not rescuing.

## Recommendations → Amend queue (owner decides)

1. **Promote the two framework-surface gates to blocking.** `ci-knowledge-index` + `ci-traceability`
   have now completed a clean advisory phase (this audit + T6.2.1/T6.2.2 detector proofs). Per §8 they
   are eligible to drop `continue-on-error` — but that flip is itself a **decision record** (not this
   task). Recommended for the Amend queue.
2. **`ci-e2e` N=3 clock is still 0** — blocked by the compose `AUTH_BOOTSTRAP_*` crash-loop finding
   (product/infra, out of framework track). Fix that finding to start the clock (owner/product).
3. **F1 (security):** owner restore-or-confirm the dropped `.env` read-denies in `settings.json`.

## Metrics baseline snapshot (blueprint §9)

Carry-forward baseline for the next audit to trend against:

```
audit_date:                       2026-07-15
gates_at_rung_ge2:                19  (18 rung-3, 1 rung-2)   share≈76% incl. 6 rung-1 golden rules
undischarged_impact_manifests:    0
past_due_freshness_contracts:     0   (oldest contract 12d — system-overview @ 2026-07-03)
l3_traceability_coverage:         12/12 = 100%   (72/72 links resolve)
escaped_defects_audit:            0
escaped_defects_accident:         3   (FS3a DI · F1 settings · stale doc figures)
drift_sweep:                      13/13 pass, 0 real drift
conformance_sweep:                6/6 conform, 0 real violation
```

## Verdict

**The framework passes its first self-audit — healthy across all five §13 signals.** Enforcement is
machine-complete where evidence warrants; no manifest is open; no contract is past-due; L3 traceability
is total; zero defects escaped into the audit. This snapshot is the **E8 metrics baseline** (blueprint
§9). Remaining work before v1.0: the regeneration-test walkthrough + `FRAMEWORK_VERSION=1.0` + opening
the Observe queue (T8.1.3).
